require "bundler/gem_tasks"

task :default => :bin

desc "Ejecuta Rspec"
task :bin do
   sh "rspec -Ilib spec/matriz_spec.rb"
end

desc "Ejecuta Test"
task :bin2 do
  sh "ruby -Ilib -Itest test/test.rb"

end









